const numbers = [1, 2, 3, 4, 5];

// start from array index 0 until 4
for (let i = 0; i < numbers.length; i++) {
  // access current element
  const number = numbers[i];

  // do something with current element
  console.log(`This is number ${number}`);
}
