export const variables = () => {
  const vehicleMake = "Tesla";
  console.log(vehicleMake);

  let vehicleSpeed = 0;
  console.log(vehicleSpeed);

  vehicleSpeed = 60;
  console.log(vehicleSpeed);
};
